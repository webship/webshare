# Drupal CMS — Failed-scenario screenshots

PNG captured by webship-js when a Drupal CMS scenario fails.
Filename pattern: `failed_{datetime}.{feature_file}.feature_{step_line}.png`.

PNGs are gitignored.
