# Drupal Standard — Cucumber reports

`cucumber.js` writes its `cucumber_report.json` (and the auto-generated
`cucumber_report.html` from webship-js's exit hook) into this folder.

These reports are produced on every run; the actual files are
gitignored.
