# Drupal CMS — Cucumber reports

`cucumber.drupalcms.js` writes its `cucumber_report.json` (and the
auto-generated `cucumber_report.html`) into this folder.

These reports are produced on every run; the actual files are
gitignored.
